# Thời gian giữ lệnh — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Thêm thời điểm đóng lệnh (`closed_at`) vào mỗi lệnh, rồi dựng thời gian giữ lệnh thành ba chỉ số KPI, một cột bảng và một biểu đồ phân phối trên dashboard.

**Architecture:** `closed_at` là cột nullable DUY NHẤT được thêm vào DB; mọi thứ khác — `hold_seconds`, ba số trung bình, sáu bucket phân phối — là trường suy diễn tính lúc đọc trong các package thuần `metrics` và `aggregate`, theo quy tắc 2 của CLAUDE.md. Dòng chảy đi từ trong ra ngoài: domain → metrics → aggregate → httpapi → frontend, mỗi tầng có test riêng và tầng dưới không biết gì về tầng trên.

**Tech Stack:** Go 1.23, chi, GORM, PostgreSQL 16, `shopspring/decimal`; Vite + React 19 + TypeScript, TanStack Query v5, shadcn/ui, Tailwind v4, Recharts, Vitest.

**Spec:** `docs/superpowers/specs/2026-09-11-hold-time-design.md`

## Global Constraints

Áp cho MỌI task dưới đây:

- **Code tiếng Anh, comment tiếng Việt** (quy tắc 9). Mọi định danh — biến, hàm, kiểu, key JSON, tên cột DB, chuỗi test — bằng tiếng Anh. Comment và tài liệu bằng tiếng Việt. Ngoại lệ: dữ liệu nghiệp vụ hiển thị cho người dùng (nhãn bucket, header CSV, text UI) vẫn tiếng Việt.
- **Tiền là `decimal.Decimal`, không bao giờ `float64`** (quy tắc 1). Thời lượng KHÔNG phải tiền: nó là `int64` giây.
- **Không lưu trường suy diễn** (quy tắc 2). `hold_seconds` và mọi số trung bình không có cột trong DB.
- **`internal/scoring`, `internal/metrics`, `internal/aggregate` là package thuần** (quy tắc 3): cấm import GORM, `net/http`, `database/sql`, `context`. Test của chúng chạy bằng `make test-pure`, không cần Docker.
- **Lưu UTC, tính theo `accounts.timezone`** (quy tắc 4). Không hardcode `+7`.
- **Soft delete** qua `deleted_at` (quy tắc 6).
- **Con trỏ nil = "không xác định", KHÁC 0.** Frontend hiện `—`, không hiện `0`.
- Test đi cùng feature trong CÙNG lần thay đổi, không dời sang task sau (mục "Testing (bắt buộc)" của CLAUDE.md).
- Node của frontend: `nvm use` (>= 20) trước mọi lệnh `npm`/`npx`.
- Lệnh kiểm: `make test-pure` (package thuần, nhanh) · `make test` (toàn bộ Go, CẦN Docker) · `make lint` · `make test-fe`.

---

### Task 1: Cột `closed_at` trong DB và domain

**Files:**
- Create: `backend/migrations/0004_trade_closed_at.up.sql`
- Create: `backend/migrations/0004_trade_closed_at.down.sql`
- Modify: `backend/internal/domain/models.go:31-67` (struct `Trade`)
- Modify: `backend/internal/domain/trade_rules.go:144-148` (khối `var` lỗi), `:158-180` (`ValidateTrade`)
- Test: `backend/internal/domain/trade_rules_test.go`

**Interfaces:**
- Consumes: chưa có gì.
- Produces:
  - `domain.Trade.ClosedAt *time.Time` — nil nghĩa là chưa đóng.
  - `domain.ErrClosedBeforeEntered error` — lỗi khi `closed_at < entered_at`.
  - `domain.ValidateTrade(*Trade) error` giữ nguyên chữ ký, thêm một luật.

- [ ] **Step 1: Viết test fail cho luật `closed_at >= entered_at`**

Thêm vào `backend/internal/domain/trade_rules_test.go`. Đọc file trước để dùng đúng helper dựng `Trade` hợp lệ đang có ở đó; nếu chưa có thì dùng literal như dưới.

```go
func TestValidateTradeClosedAt(t *testing.T) {
	entered := time.Date(2026, 9, 10, 14, 0, 0, 0, time.UTC)
	before := entered.Add(-time.Second)
	after := entered.Add(90 * time.Minute)
	equal := entered

	base := func() domain.Trade {
		return domain.Trade{
			EnteredAt: entered,
			Symbol:    "XAUUSD",
			Direction: "Long",
		}
	}

	tests := []struct {
		name     string
		closedAt *time.Time
		wantErr  error
	}{
		{name: "nil thi hop le — lenh chua dong", closedAt: nil},
		{name: "sau entered_at thi hop le", closedAt: &after},
		{name: "bang entered_at thi hop le — vao ra trong cung mot giay", closedAt: &equal},
		{name: "truoc entered_at thi bi tu choi", closedAt: &before, wantErr: domain.ErrClosedBeforeEntered},
	}

	for _, tc := range tests {
		t.Run(tc.name, func(t *testing.T) {
			tr := base()
			tr.ClosedAt = tc.closedAt
			err := domain.ValidateTrade(&tr)
			if tc.wantErr == nil {
				require.NoError(t, err)
				return
			}
			require.ErrorIs(t, err, tc.wantErr)
		})
	}
}
```

- [ ] **Step 2: Chạy test, xác nhận FAIL**

```bash
cd backend && go test ./internal/domain/... -run TestValidateTradeClosedAt -v
```

Expected: FAIL — `tr.ClosedAt undefined` và `domain.ErrClosedBeforeEntered undefined` (lỗi biên dịch).

- [ ] **Step 3: Thêm trường `ClosedAt` vào `domain.Trade`**

Trong `backend/internal/domain/models.go`, ngay sau dòng `EnteredAt time.Time`:

```go
	EnteredAt time.Time `gorm:"column:entered_at"` // luôn UTC
	// ClosedAt là thời điểm ĐÓNG lệnh, nil khi lệnh còn đang chạy hoặc khi dữ
	// liệu cũ chưa có. Thời gian giữ lệnh = ClosedAt − EnteredAt, và nó là
	// trường SUY DIỄN (metrics.Enriched.HoldSeconds), không có cột trong DB.
	//
	// Con trỏ chứ không phải time.Time, cùng lý do đã ghi ở Entry/Exit/Volume
	// bên dưới: cột này NULLable, mà giá trị rỗng của time.Time không phải
	// NULL — nó là năm 0001. Dùng kiểu giá trị thì "chưa đóng lệnh" sẽ lặng lẽ
	// được ghi thành "đóng lệnh năm 0001", và thời gian giữ lệnh của mọi lệnh
	// cũ sẽ ra khoảng hai nghìn năm mà không một lỗi nào báo.
	ClosedAt *time.Time `gorm:"column:closed_at"`
```

- [ ] **Step 4: Thêm lỗi và luật vào `trade_rules.go`**

Trong khối `var` lỗi (cạnh `ErrEnteredAtEmpty`):

```go
	ErrClosedBeforeEntered = fmt.Errorf("thời điểm đóng lệnh không được trước thời điểm vào lệnh")
```

Trong `ValidateTrade`, ngay sau khối kiểm `t.EnteredAt.IsZero()`:

```go
	// Chỉ kiểm khi CÓ giá trị: lệnh đang chạy thì nil là trạng thái hợp lệ,
	// không phải thiếu dữ liệu. Bằng nhau thì hợp lệ — một lệnh vào và ra
	// trong cùng một giây là chuyện có thật, nhất là với lệnh scalp.
	if t.ClosedAt != nil && t.ClosedAt.Before(t.EnteredAt) {
		return ErrClosedBeforeEntered
	}
```

- [ ] **Step 5: Chạy test, xác nhận PASS**

```bash
cd backend && go test ./internal/domain/... -run TestValidateTradeClosedAt -v
```

Expected: PASS, cả 4 case.

- [ ] **Step 6: Viết migration**

`backend/migrations/0004_trade_closed_at.up.sql`:

```sql
-- Thời điểm đóng lệnh. NULLable vì hai lý do, và cả hai đều là trạng thái hợp
-- lệ chứ không phải dữ liệu thiếu: lệnh còn đang chạy thì chưa có giờ đóng, và
-- mọi lệnh ghi trước migration này không có thông tin đó.
--
-- Không backfill bằng entered_at: làm vậy sẽ tạo ra một loạt lệnh có thời gian
-- giữ bằng 0, kéo mọi số trung bình xuống — một con số sai trông y như một con
-- số thật. Để NULL thì các lệnh đó bị LOẠI khỏi số trung bình, đúng sự thật.
--
-- Không có CHECK (closed_at >= entered_at) ở đây: luật đó sống ở
-- domain.ValidateTrade, dùng chung cho cả ba đường vào (API, PATCH, import).
-- Thêm ràng buộc ở DB nữa là dựng nguồn sự thật thứ hai, và hai nguồn sẽ trôi
-- lệch nhau — cùng nguyên tắc đã ghi ở comment của ValidateTrade.
ALTER TABLE trades ADD COLUMN closed_at TIMESTAMPTZ;
```

`backend/migrations/0004_trade_closed_at.down.sql`:

```sql
ALTER TABLE trades DROP COLUMN closed_at;
```

- [ ] **Step 7: Chạy migration và toàn bộ test Go**

```bash
make migrate
make test
make lint
```

Expected: migration chạy sạch; toàn bộ test PASS; `gofmt`/`go vet` không báo gì.

- [ ] **Step 8: Commit**

```bash
git add backend/migrations/0004_trade_closed_at.up.sql \
        backend/migrations/0004_trade_closed_at.down.sql \
        backend/internal/domain/models.go \
        backend/internal/domain/trade_rules.go \
        backend/internal/domain/trade_rules_test.go
git commit -m "feat(be): cột closed_at và luật closed_at >= entered_at

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: `hold_seconds` trong `metrics.Enriched`

**Files:**
- Modify: `backend/internal/metrics/enrich.go:28-56` (struct `Enriched`), và thân hàm `Enrich`
- Test: `backend/internal/metrics/enrich_test.go`

**Interfaces:**
- Consumes: `domain.Trade.ClosedAt *time.Time` (Task 1).
- Produces: `metrics.Enriched.HoldSeconds *int64` với json tag `hold_seconds`. Đơn vị GIÂY. nil khi `ClosedAt == nil`.

- [ ] **Step 1: Viết test fail**

Thêm vào `backend/internal/metrics/enrich_test.go`. Đọc file trước để dùng đúng helper dựng account/trade đang có ở đó.

```go
func TestEnrichHoldSeconds(t *testing.T) {
	acc := domain.Account{ID: 1, Timezone: "Asia/Ho_Chi_Minh"}
	entered := time.Date(2026, 9, 10, 14, 0, 0, 0, time.UTC)
	closed := entered.Add(13*time.Minute + 6*time.Second)

	rows, err := metrics.Enrich([]domain.Trade{
		{AccountID: 1, STT: 1, EnteredAt: entered, Symbol: "XAUUSD", Direction: "Long", ClosedAt: &closed},
		{AccountID: 1, STT: 2, EnteredAt: entered, Symbol: "XAUUSD", Direction: "Long", ClosedAt: nil},
	}, acc)
	require.NoError(t, err)

	require.NotNil(t, rows[0].HoldSeconds)
	require.Equal(t, int64(786), *rows[0].HoldSeconds)

	// Lệnh chưa đóng: nil, KHÔNG phải 0. Số 0 ở đây đọc thành "vào ra tức
	// thì", tức một câu trả lời sai trông hoàn toàn bình thường.
	require.Nil(t, rows[1].HoldSeconds)
}
```

- [ ] **Step 2: Chạy test, xác nhận FAIL**

```bash
cd backend && go test ./internal/metrics/... -run TestEnrichHoldSeconds -v
```

Expected: FAIL — `rows[0].HoldSeconds undefined` (lỗi biên dịch).

- [ ] **Step 3: Thêm trường vào `Enriched`**

Trong `backend/internal/metrics/enrich.go`, đặt cạnh nhóm `Day`/`Week`/`Month` (chúng cũng là trường suy diễn từ thời gian):

```go
	// HoldSeconds là thời gian giữ lệnh tính bằng GIÂY, nil khi lệnh chưa có
	// ClosedAt.
	//
	// int64 giây chứ không phải time.Duration: Duration marshal ra JSON thành
	// một số nanosecond mười mấy chữ số, frontend đọc được nhưng không ai đọc
	// được. Cũng không phải decimal.Decimal — quy tắc 1 nói về TIỀN, còn đây
	// là một số đếm, không có phép chia tiền nào đi qua nó.
	HoldSeconds *int64 `json:"hold_seconds"`
```

- [ ] **Step 4: Tính trong `Enrich`**

Trong vòng lặp của `Enrich`, cạnh chỗ gán `Day`/`Week`/`Month` (đọc file để đặt đúng chỗ, biến row đang dựng có thể tên khác):

```go
		// Không ép về UTC trước khi trừ: time.Sub làm việc trên thời điểm
		// tuyệt đối, không quan tâm location — hai mốc cùng lưu UTC nên hiệu
		// luôn đúng dù location của giá trị đọc lên từ DB là gì.
		if t.ClosedAt != nil {
			secs := int64(t.ClosedAt.Sub(t.EnteredAt).Seconds())
			e.HoldSeconds = &secs
		}
```

- [ ] **Step 5: Chạy test, xác nhận PASS**

```bash
cd backend && go test ./internal/metrics/... -run TestEnrichHoldSeconds -v
make test-pure
```

Expected: PASS cả hai.

- [ ] **Step 6: Commit**

```bash
git add backend/internal/metrics/enrich.go backend/internal/metrics/enrich_test.go
git commit -m "feat(be): hold_seconds là trường suy diễn của Enriched

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: Ba chỉ số thời gian giữ trong `metrics.KPI`

**Files:**
- Modify: `backend/internal/metrics/kpi.go:12-47` (struct `KPI`), và thân `ComputeKPI`
- Test: `backend/internal/metrics/kpi_test.go`

**Interfaces:**
- Consumes: `metrics.Enriched.HoldSeconds *int64` (Task 2).
- Produces: `KPI.AvgHoldSeconds`, `KPI.AvgHoldSecondsWin`, `KPI.AvgHoldSecondsLoss` — cả ba `*int64`, đơn vị GIÂY, nil khi không có lệnh nào đủ điều kiện.

- [ ] **Step 1: Viết test fail**

Thêm vào `backend/internal/metrics/kpi_test.go`. Đọc file trước để dùng đúng helper dựng `Enriched` đang có; nếu phải dựng tay thì `Net` là trường quyết định thắng/thua.

```go
func TestComputeKPIAvgHoldSeconds(t *testing.T) {
	acc := domain.Account{ID: 1, InitialBalance: decimal.NewFromInt(10000), RiskPerTrade: decimal.NewFromFloat(0.01)}
	sec := func(n int64) *int64 { return &n }

	rows := []metrics.Enriched{
		// thắng, giữ 100s
		{Net: decimal.NewFromInt(50), HoldSeconds: sec(100)},
		// thắng, giữ 200s
		{Net: decimal.NewFromInt(30), HoldSeconds: sec(200)},
		// thua, giữ 900s — lệnh thua bị ôm lâu hơn hẳn
		{Net: decimal.NewFromInt(-40), HoldSeconds: sec(900)},
		// hoà: vào số trung bình CHUNG, không vào win cũng không vào loss
		{Net: decimal.Zero, HoldSeconds: sec(400)},
		// chưa đóng: KHÔNG vào số nào cả
		{Net: decimal.NewFromInt(70), HoldSeconds: nil},
	}

	k := metrics.ComputeKPI(rows, rows, acc, nil)

	// (100+200+900+400)/4 = 400. Lệnh chưa đóng không góp mặt ở tử lẫn mẫu.
	require.NotNil(t, k.AvgHoldSeconds)
	require.Equal(t, int64(400), *k.AvgHoldSeconds)

	require.NotNil(t, k.AvgHoldSecondsWin)
	require.Equal(t, int64(150), *k.AvgHoldSecondsWin)

	require.NotNil(t, k.AvgHoldSecondsLoss)
	require.Equal(t, int64(900), *k.AvgHoldSecondsLoss)
}

func TestComputeKPIAvgHoldSecondsNilKhiKhongCoLenhDong(t *testing.T) {
	acc := domain.Account{ID: 1, InitialBalance: decimal.NewFromInt(10000), RiskPerTrade: decimal.NewFromFloat(0.01)}

	rows := []metrics.Enriched{
		{Net: decimal.NewFromInt(50), HoldSeconds: nil},
		{Net: decimal.NewFromInt(-20), HoldSeconds: nil},
	}

	k := metrics.ComputeKPI(rows, rows, acc, nil)

	// nil chứ không phải 0: "chưa lệnh nào có giờ đóng" khác hẳn "giữ lệnh
	// trung bình 0 giây".
	require.Nil(t, k.AvgHoldSeconds)
	require.Nil(t, k.AvgHoldSecondsWin)
	require.Nil(t, k.AvgHoldSecondsLoss)
}
```

- [ ] **Step 2: Chạy test, xác nhận FAIL**

```bash
cd backend && go test ./internal/metrics/... -run 'TestComputeKPIAvgHold' -v
```

Expected: FAIL — `k.AvgHoldSeconds undefined` (lỗi biên dịch).

- [ ] **Step 3: Thêm ba trường vào `KPI`**

Trong `backend/internal/metrics/kpi.go`, đặt sau `Expectancy`:

```go
	// Thời gian giữ lệnh trung bình, tính bằng GIÂY, chỉ trên các lệnh CÓ
	// closed_at. Lệnh còn đang chạy bị loại khỏi cả tử lẫn mẫu.
	//
	// Vì sao không tính lệnh đang chạy bằng now − entered_at: con số đó đổi
	// mỗi lần tải lại trang, nên hai lần xem cùng một bộ lọc ra hai kết quả.
	// Một chỉ số không tái lập được thì không ra quyết định được.
	//
	// Win/Loss chia theo dấu của Net, khớp với WinCount/LossCount ở trên: lệnh
	// hoà (Net = 0) vào AvgHoldSeconds nhưng không vào hai số kia.
	AvgHoldSeconds     *int64
	AvgHoldSecondsWin  *int64
	AvgHoldSecondsLoss *int64
```

- [ ] **Step 4: Cộng dồn trong `ComputeKPI`**

Khai biến đếm cạnh `maxPeak, maxDD` (trước vòng lặp `for _, r := range filtered`):

```go
	var holdSum, holdWinSum, holdLossSum int64
	var holdCount, holdWinCount, holdLossCount int
```

Trong vòng lặp `for _, r := range filtered`, thêm vào cuối thân vòng lặp:

```go
		if r.HoldSeconds != nil {
			holdSum += *r.HoldSeconds
			holdCount++
			switch {
			case r.Net.IsPositive():
				holdWinSum += *r.HoldSeconds
				holdWinCount++
			case r.Net.IsNegative():
				holdLossSum += *r.HoldSeconds
				holdLossCount++
			}
			// Net == 0: chỉ vào số trung bình chung, đúng như cách WinCount và
			// LossCount đang bỏ qua lệnh hoà.
		}
```

Sau vòng lặp, cạnh chỗ tính các con trỏ khác:

```go
	k.AvgHoldSeconds = avgHold(holdSum, holdCount)
	k.AvgHoldSecondsWin = avgHold(holdWinSum, holdWinCount)
	k.AvgHoldSecondsLoss = avgHold(holdLossSum, holdLossCount)
```

Thêm hàm helper ở cuối file:

```go
// avgHold trả trung bình nguyên (chia lấy phần nguyên) hoặc nil khi mẫu bằng 0.
//
// nil chứ không phải 0, theo đúng quy ước con trỏ của KPI: "chưa có lệnh nào
// đóng" và "giữ lệnh trung bình 0 giây" là hai câu khác nhau, và frontend phải
// phân biệt được để hiện "—".
//
// Phần lẻ dưới một giây bị cắt bỏ, chủ ý: hiển thị làm tròn tới 0,1 phút nên
// một giây lẻ không bao giờ nhìn thấy được.
func avgHold(sum int64, count int) *int64 {
	if count == 0 {
		return nil
	}
	avg := sum / int64(count)
	return &avg
}
```

- [ ] **Step 5: Chạy test, xác nhận PASS**

```bash
cd backend && go test ./internal/metrics/... -run 'TestComputeKPIAvgHold' -v
make test-pure
```

Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add backend/internal/metrics/kpi.go backend/internal/metrics/kpi_test.go
git commit -m "feat(be): ba chỉ số thời gian giữ lệnh trung bình trong KPI

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: Biểu đồ phân phối theo thời gian giữ (`aggregate`)

**Files:**
- Create: `backend/internal/aggregate/holddist.go`
- Create: `backend/internal/aggregate/holddist_test.go`
- Modify: `backend/internal/aggregate/charts.go:47-71` (struct `Charts`), `:179-206` (hàm `All`)

**Interfaces:**
- Consumes: `metrics.Enriched.HoldSeconds *int64` (Task 2).
- Produces:
  - `aggregate.HoldBucket{Label string; Count, Wins, Losses int; SumNet decimal.Decimal}` với json tag `label`, `count`, `wins`, `losses`, `sum_net`.
  - `aggregate.HoldDistribution(rows []metrics.Enriched) []HoldBucket` — LUÔN trả đủ 6 bucket theo thứ tự cố định, kể cả bucket rỗng.
  - `Charts.HoldDistribution []HoldBucket` với json tag `hold_distribution`.

- [ ] **Step 1: Viết test fail**

`backend/internal/aggregate/holddist_test.go`:

```go
package aggregate_test

import (
	"testing"

	"github.com/shopspring/decimal"
	"github.com/stretchr/testify/require"

	"journal/internal/aggregate"
	"journal/internal/metrics"
)

func sec(n int64) *int64 { return &n }

func TestHoldDistributionBienBucket(t *testing.T) {
	// Mỗi case là một giá trị NGAY TẠI biên. Khoảng là nửa mở lo <= x < hi,
	// nên đúng 300 giây phải rơi vào bucket THỨ HAI, không phải bucket đầu.
	tests := []struct {
		name      string
		hold      int64
		wantLabel string
	}{
		{name: "0s", hold: 0, wantLabel: "< 5m"},
		{name: "299s ngay duoi bien 5m", hold: 299, wantLabel: "< 5m"},
		{name: "300s dung bien 5m", hold: 300, wantLabel: "5m – 15m"},
		{name: "899s", hold: 899, wantLabel: "5m – 15m"},
		{name: "900s dung bien 15m", hold: 900, wantLabel: "15m – 1h"},
		{name: "3599s", hold: 3599, wantLabel: "15m – 1h"},
		{name: "3600s dung bien 1h", hold: 3600, wantLabel: "1h – 4h"},
		{name: "14399s", hold: 14399, wantLabel: "1h – 4h"},
		{name: "14400s dung bien 4h", hold: 14400, wantLabel: "4h – 1 ngày"},
		{name: "86399s", hold: 86399, wantLabel: "4h – 1 ngày"},
		{name: "86400s dung bien 1 ngay", hold: 86400, wantLabel: "> 1 ngày"},
	}

	for _, tc := range tests {
		t.Run(tc.name, func(t *testing.T) {
			got := aggregate.HoldDistribution([]metrics.Enriched{
				{Net: decimal.NewFromInt(10), HoldSeconds: sec(tc.hold)},
			})

			// Đúng MỘT bucket có lệnh, và nó phải là bucket mong đợi: khẳng
			// định này bắt cả trường hợp lệnh bị đếm hai lần lẫn lọt khe.
			var hit []string
			for _, b := range got {
				if b.Count > 0 {
					hit = append(hit, b.Label)
				}
			}
			require.Equal(t, []string{tc.wantLabel}, hit)
		})
	}
}

func TestHoldDistributionLuonDuSauBucket(t *testing.T) {
	got := aggregate.HoldDistribution(nil)

	require.Len(t, got, 6)
	require.Equal(t, []string{"< 5m", "5m – 15m", "15m – 1h", "1h – 4h", "4h – 1 ngày", "> 1 ngày"},
		[]string{got[0].Label, got[1].Label, got[2].Label, got[3].Label, got[4].Label, got[5].Label})
	for _, b := range got {
		require.Zero(t, b.Count)
		require.True(t, b.SumNet.IsZero())
	}
}

func TestHoldDistributionBoQuaLenhChuaDong(t *testing.T) {
	got := aggregate.HoldDistribution([]metrics.Enriched{
		{Net: decimal.NewFromInt(10), HoldSeconds: sec(60)},
		{Net: decimal.NewFromInt(99), HoldSeconds: nil},
	})

	total := 0
	for _, b := range got {
		total += b.Count
	}
	// Lệnh chưa đóng không vào bucket nào — nếu nó lọt vào một bucket thì
	// tổng sẽ là 2, và biểu đồ sẽ kể một câu chuyện về lệnh mà nó không biết
	// gì về thời gian giữ.
	require.Equal(t, 1, total)
}

func TestHoldDistributionThangThuaVaSumNet(t *testing.T) {
	got := aggregate.HoldDistribution([]metrics.Enriched{
		{Net: decimal.NewFromInt(30), HoldSeconds: sec(60)},
		{Net: decimal.NewFromInt(-10), HoldSeconds: sec(120)},
		{Net: decimal.Zero, HoldSeconds: sec(180)},
	})

	first := got[0]
	require.Equal(t, 3, first.Count)
	require.Equal(t, 1, first.Wins)
	require.Equal(t, 1, first.Losses)
	// Lệnh hoà đếm vào Count nhưng không vào Wins cũng không vào Losses —
	// cùng quy ước với RDistribution.
	require.Equal(t, "20", first.SumNet.String())
}
```

- [ ] **Step 2: Chạy test, xác nhận FAIL**

```bash
cd backend && go test ./internal/aggregate/... -run TestHoldDistribution -v
```

Expected: FAIL — `undefined: aggregate.HoldDistribution`.

- [ ] **Step 3: Viết `holddist.go`**

```go
package aggregate

import (
	"github.com/shopspring/decimal"

	"journal/internal/metrics"
)

// HoldBucket là một cột của histogram thời gian giữ lệnh.
//
// Có SumNet, khác RBucket: câu hỏi thật không phải "tôi hay giữ lệnh bao lâu"
// mà "khoảng giữ lệnh nào SINH LỜI". Một bucket đông lệnh nhưng âm tiền là
// đúng thứ cần nhìn thấy, và chỉ đếm số lệnh thì không thấy được.
type HoldBucket struct {
	Label  string          `json:"label"`
	Count  int             `json:"count"`
	Wins   int             `json:"wins"`
	Losses int             `json:"losses"`
	SumNet decimal.Decimal `json:"sum_net"`
}

// holdBucketDef mô tả một khoảng thời gian giữ lệnh, đơn vị GIÂY. Khoảng là
// nửa mở: lo <= x < hi. hasLo/hasHi = false nghĩa là vô cực về phía đó.
//
// Biên khai bằng int64 nên so sánh chính xác tuyệt đối, không có phép quy đổi
// dấu phẩy động nào lọt vào — cùng nguyên tắc như rBucketDef ở rdist.go.
type holdBucketDef struct {
	label string
	lo    int64
	hi    int64
	hasLo bool
	hasHi bool
}

// Sáu khoảng chọn theo nhịp giao dịch trong ngày: dưới 5 phút là scalp, trên
// một ngày là lệnh qua đêm. Nhãn tiếng Việt vì đây là DỮ LIỆU hiển thị cho
// người dùng, không phải định danh code (quy tắc 9).
var holdBucketDefs = []holdBucketDef{
	{label: "< 5m", hi: 300, hasHi: true},
	{label: "5m – 15m", lo: 300, hi: 900, hasLo: true, hasHi: true},
	{label: "15m – 1h", lo: 900, hi: 3600, hasLo: true, hasHi: true},
	{label: "1h – 4h", lo: 3600, hi: 14400, hasLo: true, hasHi: true},
	{label: "4h – 1 ngày", lo: 14400, hi: 86400, hasLo: true, hasHi: true},
	{label: "> 1 ngày", lo: 86400, hasLo: true},
}

// HoldDistribution đếm lệnh theo khoảng thời gian giữ.
//
// LUÔN trả đủ sáu bucket kể cả khi rỗng: biểu đồ phải giữ nguyên trục qua mọi
// bộ lọc, nếu không thì đổi tháng sẽ làm các cột nhảy chỗ.
//
// Lệnh không có HoldSeconds (chưa đóng, hoặc dữ liệu cũ) KHÔNG vào bucket nào.
// Xếp chúng vào bucket đầu sẽ là bịa ra một thời gian giữ bằng 0.
func HoldDistribution(rows []metrics.Enriched) []HoldBucket {
	buckets := make([]HoldBucket, len(holdBucketDefs))
	for i, d := range holdBucketDefs {
		buckets[i] = HoldBucket{Label: d.label, SumNet: decimal.Zero}
	}

	for _, r := range rows {
		if r.HoldSeconds == nil {
			continue
		}
		idx := holdBucketIndex(*r.HoldSeconds)
		if idx < 0 {
			continue
		}
		buckets[idx].Count++
		buckets[idx].SumNet = buckets[idx].SumNet.Add(r.Net)
		switch {
		case r.Net.IsPositive():
			buckets[idx].Wins++
		case r.Net.IsNegative():
			buckets[idx].Losses++
		}
		// net == 0: chỉ tăng Count, không phải Win cũng không phải Loss.
	}
	return buckets
}

// holdBucketIndex trả chỉ số bucket chứa giá trị, hoặc -1 nếu không bucket nào
// chứa. Sáu khoảng phủ kín từ 0 tới vô cực nên -1 chỉ xảy ra với giá trị ÂM,
// vốn đã bị domain.ValidateTrade chặn — giữ nhánh này để một dữ liệu hỏng lọt
// qua bằng đường khác không âm thầm rơi vào bucket đầu.
func holdBucketIndex(seconds int64) int {
	for i, d := range holdBucketDefs {
		if d.hasLo && seconds < d.lo {
			continue
		}
		if d.hasHi && seconds >= d.hi {
			continue
		}
		return i
	}
	return -1
}
```

- [ ] **Step 4: Chạy test, xác nhận PASS**

```bash
cd backend && go test ./internal/aggregate/... -run TestHoldDistribution -v
```

Expected: PASS, tất cả case.

- [ ] **Step 5: Nối vào `Charts`**

Trong `backend/internal/aggregate/charts.go`, thêm vào struct `Charts` ngay sau `RDistribution`:

```go
	HoldDistribution []HoldBucket `json:"hold_distribution"`
```

Trong hàm `All`, cạnh `RDistribution`:

```go
		HoldDistribution: HoldDistribution(filtered),
```

Dùng `filtered` chứ không phải `all`: đây là một aggregation §5 bình thường, không thuộc ngoại lệ của quy tắc 8 (chỉ streak và current_balance thuộc ngoại lệ đó).

- [ ] **Step 6: Chạy test thuần, xác nhận PASS**

```bash
make test-pure
make lint
```

Expected: PASS. (Test của `httpapi` sẽ ĐỎ ở bước này vì golden file chưa có trường mới — Task 5 sửa.)

- [ ] **Step 7: Commit**

```bash
git add backend/internal/aggregate/holddist.go \
        backend/internal/aggregate/holddist_test.go \
        backend/internal/aggregate/charts.go
git commit -m "feat(be): phân phối lệnh theo khoảng thời gian giữ

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: Hợp đồng HTTP — `closed_at` vào/ra, `hold_seconds` và KPI mới ra

**Files:**
- Modify: `backend/internal/httpapi/trade_dto.go:30-56` (`tradeDTO`), `:64-92` (`toTradeDTO`), `:159-207` (`statsDTO`), `:209-224` (`toStatsDTO`), `:215-246` (`tradeCreateRequest` + `toInput`), `:247-282` (`tradePatchRequest` + `toPatch`)
- Modify: `backend/internal/service/trade.go:35-52` (`TradeInput`), `:121-147` (`tradeFromInput`), `:231-249` (`TradePatch`), `:272-300` (`patchToFields`)
- Modify: `backend/internal/httpapi/testdata/charts.golden.json`
- Test: `backend/internal/httpapi/trade_handler_test.go`, `backend/internal/service/trade_test.go`

**Interfaces:**
- Consumes: `domain.Trade.ClosedAt` (T1), `metrics.Enriched.HoldSeconds` (T2), `KPI.AvgHoldSeconds*` (T3), `Charts.HoldDistribution` (T4).
- Produces (hợp đồng JSON mà frontend dựa vào):
  - `GET /trades` item: `"closed_at": string|null` (RFC3339 UTC), `"hold_seconds": number|null`.
  - `GET /stats`: `"avg_hold_seconds"`, `"avg_hold_seconds_win"`, `"avg_hold_seconds_loss"` — `number|null`.
  - `POST /trades` body nhận `"closed_at": string|null`.
  - `PATCH /trades/{id}` body nhận `"closed_at"` ba trạng thái (vắng / null / giá trị).
  - `service.TradeInput.ClosedAt *time.Time`, `service.TradePatch.ClosedAt Tristate[time.Time]`.

- [ ] **Step 1: Viết test fail cho vòng đời API của `closed_at`**

Thêm vào `backend/internal/httpapi/trade_handler_test.go`. Đọc file trước để dùng đúng helper dựng server/account/request đang có ở đó, và đặt tên test theo khuôn của file.

```go
func TestTradeClosedAtVongDoiAPI(t *testing.T) {
	// Tạo lệnh CÓ closed_at → đọc lại thấy cả closed_at lẫn hold_seconds.
	// Rồi PATCH về null → cả hai thành null. Rồi PATCH đặt lại → cả hai trở lại.
	//
	// Ba chặng trong MỘT test vì chúng là một vòng đời: tách ra thì mỗi chặng
	// phải tự dựng lại trạng thái, và chặng "xoá về null" sẽ không còn khẳng
	// định được rằng nó xoá được một giá trị ĐÃ CÓ.
	env := newTestEnv(t) // dùng helper sẵn có của file này
	accID := env.seedAccount(t)

	created := env.postJSON(t, "/api/accounts/"+accID+"/trades", map[string]any{
		"entered_at": "2026-09-10T14:00:00Z",
		"closed_at":  "2026-09-10T14:13:06Z",
		"symbol":     "XAUUSD",
		"direction":  "Long",
		"profit":     "100",
	})
	require.Equal(t, http.StatusCreated, created.Code)

	item := env.firstTrade(t, accID)
	require.Equal(t, "2026-09-10T14:13:06Z", item["closed_at"])
	require.Equal(t, float64(786), item["hold_seconds"])

	id := int64(item["id"].(float64))

	// null = XOÁ giá trị, khác hẳn với khoá vắng mặt (giữ nguyên).
	env.patchJSON(t, "/api/trades/"+strconv.FormatInt(id, 10), map[string]any{"closed_at": nil})
	item = env.firstTrade(t, accID)
	require.Nil(t, item["closed_at"])
	require.Nil(t, item["hold_seconds"])

	env.patchJSON(t, "/api/trades/"+strconv.FormatInt(id, 10), map[string]any{"closed_at": "2026-09-10T15:00:00Z"})
	item = env.firstTrade(t, accID)
	require.Equal(t, "2026-09-10T15:00:00Z", item["closed_at"])
	require.Equal(t, float64(3600), item["hold_seconds"])
}

func TestTradeClosedAtTruocEnteredAtBi400(t *testing.T) {
	env := newTestEnv(t)
	accID := env.seedAccount(t)

	res := env.postJSON(t, "/api/accounts/"+accID+"/trades", map[string]any{
		"entered_at": "2026-09-10T14:00:00Z",
		"closed_at":  "2026-09-10T13:00:00Z",
		"symbol":     "XAUUSD",
		"direction":  "Long",
		"profit":     "100",
	})

	require.Equal(t, http.StatusBadRequest, res.Code)
}
```

- [ ] **Step 2: Chạy test, xác nhận FAIL**

```bash
cd backend && go test ./internal/httpapi/... -run TestTradeClosedAt -v
```

Expected: FAIL — `DisallowUnknownFields` đang bật nên `closed_at` trong body cho 400 ở test đầu; test thứ hai có thể "pass vì lý do sai", nên test đầu là cái quan trọng.

- [ ] **Step 3: Thêm `ClosedAt` vào `service.TradeInput` và `tradeFromInput`**

`backend/internal/service/trade.go`, trong `TradeInput` ngay sau `EnteredAt`:

```go
	ClosedAt       *time.Time
```

Trong `tradeFromInput`, trong literal `domain.Trade{}` ngay sau `EnteredAt: in.EnteredAt,`:

```go
		ClosedAt:       in.ClosedAt,
```

Và sau dòng `t.EnteredAt = t.EnteredAt.UTC()` (chuẩn hoá UTC PHẢI sau ValidateTrade, cùng lý do đã ghi ở comment sẵn có):

```go
	if t.ClosedAt != nil {
		utc := t.ClosedAt.UTC()
		t.ClosedAt = &utc
	}
```

- [ ] **Step 4: Thêm `ClosedAt` vào `service.TradePatch` và `patchToFields`**

Trong `TradePatch` ngay sau `EnteredAt`:

```go
	ClosedAt       Tristate[time.Time]
```

Trong `patchToFields`, ngay sau khối `p.EnteredAt`:

```go
	// Ba trạng thái ở đây đều CÓ NGHĨA, khác với entered_at ngay trên:
	// vắng mặt = giữ nguyên, null = lệnh chưa đóng (hoặc gỡ giờ đóng nhập
	// nhầm), có giá trị = đặt giờ đóng. Nên null đi thẳng xuống DB thành NULL
	// chứ không phải một lỗi validation.
	if v, ok := p.ClosedAt.Get(); ok {
		if v == nil {
			f["closed_at"] = nil
		} else {
			f["closed_at"] = v.UTC()
		}
	}
```

**Lưu ý quan trọng về luật `closed_at >= entered_at` trên đường PATCH:** `patchToFields` chỉ thấy các trường ĐƯỢC GỬI, nên nó không tự kiểm được quan hệ giữa hai mốc khi chỉ một trong hai có mặt. Thực thi luật này trong `TradeService.Update`: đọc lệnh hiện tại bằng `s.ByID`, ghép giá trị mới đè lên giá trị cũ, rồi so sánh. Đặt đoạn kiểm ngay sau khi `patchToFields` trả về map và trước khi gọi store:

```go
	// Luật closed_at >= entered_at phải kiểm trên trạng thái SAU khi ghép, vì
	// PATCH có thể chỉ gửi MỘT trong hai mốc: sửa mỗi entered_at về sau giờ
	// đóng cũng tạo ra thời gian giữ lệnh âm, y như sửa mỗi closed_at.
	cur, err := s.ByID(ctx, id)
	if err != nil {
		return err
	}
	entered := cur.EnteredAt
	if v, ok := f["entered_at"]; ok {
		entered = v.(time.Time)
	}
	closed := cur.ClosedAt
	if v, ok := f["closed_at"]; ok {
		if v == nil {
			closed = nil
		} else {
			ts := v.(time.Time)
			closed = &ts
		}
	}
	if closed != nil && closed.Before(entered) {
		return apperr.Validation(domain.ErrClosedBeforeEntered.Error())
	}
```

- [ ] **Step 5: Thêm `closed_at` vào các DTO**

`backend/internal/httpapi/trade_dto.go`:

Trong `tradeDTO`, sau `EnteredAt`:

```go
	// Con trỏ chuỗi: null khi lệnh chưa đóng. Chuỗi rỗng "" sẽ bị frontend
	// đọc thành một giá trị có thật nhưng rỗng, còn null thì không mơ hồ.
	ClosedAt *string `json:"closed_at"`
```

Trong `toTradeDTO`, sau dòng gán `EnteredAt`:

```go
		ClosedAt: rfc3339Ptr(t.ClosedAt),
```

Thêm helper ở cuối file:

```go
// rfc3339Ptr đổi mốc thời gian tuỳ chọn thành chuỗi RFC3339 UTC, giữ nguyên
// nil. Cùng quy ước với EnteredAt: gửi kèm offset là điều kiện để frontend đổi
// được sang giờ account.
func rfc3339Ptr(t *time.Time) *string {
	if t == nil {
		return nil
	}
	s := t.UTC().Format(time.RFC3339)
	return &s
}
```

Trong `tradeCreateRequest`, sau `EnteredAt`:

```go
	ClosedAt       *time.Time       `json:"closed_at"`
```

Trong `toInput()`, thêm `ClosedAt: r.ClosedAt,` vào literal.

Trong `tradePatchRequest`, sau `EnteredAt`:

```go
	ClosedAt       service.Tristate[time.Time]       `json:"closed_at"`
```

Trong `toPatch()`, thêm `ClosedAt: r.ClosedAt,` vào literal.

Trong `statsDTO`, sau `Expectancy`:

```go
	AvgHoldSeconds     *int64 `json:"avg_hold_seconds"`
	AvgHoldSecondsWin  *int64 `json:"avg_hold_seconds_win"`
	AvgHoldSecondsLoss *int64 `json:"avg_hold_seconds_loss"`
```

Trong `toStatsDTO`, thêm vào literal:

```go
		AvgHoldSeconds: k.AvgHoldSeconds, AvgHoldSecondsWin: k.AvgHoldSecondsWin, AvgHoldSecondsLoss: k.AvgHoldSecondsLoss,
```

- [ ] **Step 6: Cập nhật golden file**

`backend/internal/httpapi/testdata/charts.golden.json` cần trường `hold_distribution`. Đọc test đang dùng file này để biết nó dựng dữ liệu gì, rồi chạy test và đọc diff mà nó in ra để điền đúng giá trị — KHÔNG đoán. Nếu test có cờ cập nhật golden (ví dụ `-update`), dùng nó, rồi ĐỌC LẠI diff của `git diff` để xác nhận thay đổi đúng bằng một trường mới, không có trường nào khác đổi giá trị.

- [ ] **Step 7: Chạy toàn bộ test Go, xác nhận PASS**

```bash
make test
make lint
```

Expected: PASS toàn bộ, kể cả hai test mới của Step 1.

- [ ] **Step 8: Commit**

```bash
git add backend/internal/httpapi/ backend/internal/service/trade.go
git commit -m "feat(be): closed_at qua API, hold_seconds và KPI thời gian giữ trong response

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: CSV — cột "Ngày đóng" thứ 19

**Files:**
- Modify: `backend/internal/csvformat/csvformat.go:23-33` (`Columns`), `:41` (`InputColumnCount`), `:60-76` (`ColumnAliases`)
- Modify: `backend/internal/importer/normalize.go` (thêm `ParseDateTime`), `backend/internal/importer/parse.go:163-167` (khối đọc `day`)
- Modify: `backend/internal/exporter/csv.go:84-92` (hàm `row`)
- Test: `backend/internal/csvformat/csvformat_test.go`, `backend/internal/importer/normalize_test.go`, `backend/internal/exporter/csv_test.go`

**Interfaces:**
- Consumes: `domain.Trade.ClosedAt` (T1).
- Produces: `importer.ParseDateTime(s string, loc *time.Location) (*time.Time, error)` — trả `(nil, nil)` cho ô rỗng; chấp nhận RFC3339 và các layout ngày-giờ thông dụng; KHÔNG cắt bỏ phần giờ.

- [ ] **Step 1: Viết test fail cho `ParseDateTime`**

Thêm vào `backend/internal/importer/normalize_test.go`:

```go
func TestParseDateTime(t *testing.T) {
	vn, err := time.LoadLocation("Asia/Ho_Chi_Minh")
	require.NoError(t, err)

	t.Run("o rong tra nil khong loi — lenh chua dong la hop le", func(t *testing.T) {
		got, err := importer.ParseDateTime("   ", vn)
		require.NoError(t, err)
		require.Nil(t, got)
	})

	t.Run("RFC3339 giu nguyen thoi diem tuyet doi", func(t *testing.T) {
		got, err := importer.ParseDateTime("2026-09-10T14:13:06Z", vn)
		require.NoError(t, err)
		require.NotNil(t, got)
		require.True(t, got.Equal(time.Date(2026, 9, 10, 14, 13, 6, 0, time.UTC)))
	})

	t.Run("chuoi khong mang offset doc theo timezone account", func(t *testing.T) {
		got, err := importer.ParseDateTime("2026-09-10 21:13:06", vn)
		require.NoError(t, err)
		require.NotNil(t, got)
		// 21:13 giờ VN = 14:13 UTC. Đọc theo UTC sẽ lệch 7 tiếng và thời gian
		// giữ lệnh sẽ sai đúng bằng chừng đó.
		require.True(t, got.Equal(time.Date(2026, 9, 10, 14, 13, 6, 0, time.UTC)))
	})

	t.Run("GIU phan gio — day la khac biet then chot so voi ParseDay", func(t *testing.T) {
		got, err := importer.ParseDateTime("10/09/2026 21:47:52", vn)
		require.NoError(t, err)
		require.NotNil(t, got)
		inVN := got.In(vn)
		require.Equal(t, 21, inVN.Hour())
		require.Equal(t, 47, inVN.Minute())
		require.Equal(t, 52, inVN.Second())
	})

	t.Run("chuoi rac bao loi", func(t *testing.T) {
		_, err := importer.ParseDateTime("hôm qua", vn)
		require.Error(t, err)
	})
}
```

- [ ] **Step 2: Chạy test, xác nhận FAIL**

```bash
cd backend && go test ./internal/importer/... -run TestParseDateTime -v
```

Expected: FAIL — `undefined: importer.ParseDateTime`.

- [ ] **Step 3: Viết `ParseDateTime`**

Trong `backend/internal/importer/normalize.go`, ngay sau `ParseDay`:

```go
// dateTimeFormats là các layout NGÀY-GIỜ được chấp nhận, thử theo thứ tự.
//
// ISO đứng đầu vì không mơ hồ. Thứ tự ngày-trước (kiểu Việt Nam) đứng trước
// tháng-trước, cùng lý do đã ghi ở dateFormats.
var dateTimeFormats = []string{
	time.RFC3339,
	"2006-01-02T15:04:05",
	"2006-01-02 15:04:05",
	"2006-01-02 15:04",
	"02/01/2006 15:04:05",
	"2/1/2006 15:04:05",
	"02/01/2006 15:04",
	"2/1/2006 15:04",
}

// ParseDateTime đọc một ô NGÀY-GIỜ thành thời điểm UTC, dùng cho closed_at.
//
// Khác ParseDay ở ĐÚNG một điểm, và đó là toàn bộ lý do nó tồn tại: ParseDay
// cố tình cắt bỏ phần giờ rồi chốt về 12:00 giờ account, vì file Excel gốc chỉ
// có cột ngày. Dùng lại nó ở đây thì mọi thời gian giữ lệnh sẽ ra 0 hoặc bội
// số của 24 giờ — cột này sinh ra để đo phút, nên phần giờ là thứ KHÔNG được
// phép mất.
//
// Ô rỗng trả (nil, nil): lệnh chưa đóng là trạng thái hợp lệ, không phải lỗi
// nhập liệu. Đây cũng là cách file CSV cũ (không có cột này) nhập được.
func ParseDateTime(s string, loc *time.Location) (*time.Time, error) {
	v := strings.TrimSpace(s)
	if v == "" {
		return nil, nil
	}
	for _, layout := range dateTimeFormats {
		// ParseInLocation chứ không Parse, cùng lý do như ParseDay: chuỗi
		// không mang offset phải đọc theo giờ account. RFC3339 tự mang offset
		// nên loc không ảnh hưởng tới nó.
		if t, err := time.ParseInLocation(layout, v, loc); err == nil {
			utc := t.UTC()
			return &utc, nil
		}
	}
	return nil, fmt.Errorf("thời điểm %q không đọc được (nhận yyyy-mm-dd hh:mm:ss hoặc dd/mm/yyyy hh:mm:ss)", strings.TrimSpace(s))
}
```

- [ ] **Step 4: Chạy test, xác nhận PASS**

```bash
cd backend && go test ./internal/importer/... -run TestParseDateTime -v
```

Expected: PASS, cả 5 case.

- [ ] **Step 5: Viết test fail cho định dạng CSV**

Thêm vào `backend/internal/csvformat/csvformat_test.go`:

```go
func TestColumnsCoNgayDong(t *testing.T) {
	cols := csvformat.Header()

	// Đặt ngay sau "Day": hai mốc thời gian của một lệnh nên đứng cạnh nhau
	// khi người dùng mở file bằng Excel.
	require.Equal(t, "Day", cols[2])
	require.Equal(t, "Ngày đóng", cols[3])

	// Cột mới là cột INPUT, nên ranh giới input/derived dịch lên một.
	require.Equal(t, 19, csvformat.InputColumnCount)

	// KHÔNG bắt buộc: file Excel gốc và mọi file đã xuất trước đây đều thiếu
	// cột này và phải tiếp tục nhập được.
	require.NotContains(t, csvformat.Required, "closed_at")
	require.Contains(t, csvformat.ColumnAliases, "closed_at")
}
```

Và một test round-trip trong `backend/internal/exporter/csv_test.go` (đọc file trước để dùng đúng helper round-trip đang có ở đó — mục đích của package `csvformat` là giữ ràng buộc này bằng cấu trúc):

```go
func TestRoundTripGiuNguyenClosedAtKeCaPhanGio(t *testing.T) {
	entered := time.Date(2026, 9, 10, 14, 0, 0, 0, time.UTC)
	closed := time.Date(2026, 9, 10, 14, 13, 6, 0, time.UTC)

	// Xuất một lệnh có giờ đóng lẻ phút lẻ giây, nhập lại, phải ra ĐÚNG thời
	// điểm đó. Nếu đường nhập dùng nhầm ParseDay thì giờ sẽ bị chốt về 12:00
	// và khẳng định dưới đây đỏ — đó chính là cái bẫy test này canh.
	got := roundTrip(t, domain.Trade{
		EnteredAt: entered, ClosedAt: &closed,
		Symbol: "XAUUSD", Direction: "Long", Profit: decimal.NewFromInt(100),
	})

	require.NotNil(t, got.ClosedAt)
	require.True(t, got.ClosedAt.Equal(closed), "mong %v, nhận %v", closed, got.ClosedAt)
}

func TestImportFileThieuCotNgayDongVanChayDuoc(t *testing.T) {
	// File Excel gốc không có cột này. Nhập vào phải ra closed_at = nil, không
	// phải một lỗi.
	csv := "STT,Day,Symbol,Long/ Short,Profit\n1,2026-09-10,XAUUSD,Long,100\n"

	rows, errs := importRows(t, csv) // dùng helper sẵn có của file test
	require.Empty(t, errs)
	require.Len(t, rows, 1)
	require.Nil(t, rows[0].ClosedAt)
}
```

- [ ] **Step 6: Chạy test, xác nhận FAIL**

```bash
cd backend && go test ./internal/csvformat/... ./internal/exporter/... ./internal/importer/... -v
```

Expected: FAIL — `cols[3]` là `"Symbol"` chứ không phải `"Ngày đóng"`; `InputColumnCount` là 18.

- [ ] **Step 7: Thêm cột vào `csvformat`**

Trong `Columns`, đổi dòng đầu thành:

```go
	"STT", "Account", "Day", "Ngày đóng", "Symbol", "Long/ Short",
```

Đổi hằng số:

```go
const InputColumnCount = 19
```

Thêm vào `ColumnAliases`, ngay sau dòng `"day"`:

```go
	"closed_at":        {"ngày đóng", "ngay dong", "close date", "closed at", "ngày giờ đóng"},
```

Cập nhật comment của `Columns` để nói rõ cột thứ 4 là dấu thời gian ĐẦY ĐỦ, khác cột `Day` ngày trần:

```go
// Cột "Ngày đóng" mang dấu thời gian ĐẦY ĐỦ (RFC3339), không phải ngày trần
// như cột "Day" ngay trước nó. Hai định dạng khác nhau trong cùng một file là
// chủ ý: "Day" giữ nguyên hình dạng của file Excel gốc để file cũ nhập được,
// còn "Ngày đóng" sinh ra để đo thời gian giữ lệnh theo PHÚT nên mất phần giờ
// là mất toàn bộ ý nghĩa của nó.
```

- [ ] **Step 8: Đọc cột mới trong importer**

Trong `backend/internal/importer/parse.go`, ngay sau khối đọc `day`:

```go
	closedAt, err := ParseDateTime(cellAt("closed_at"), loc)
	if err != nil {
		return t, rowErr("closed_at", err)
	}
	t.ClosedAt = closedAt
```

Luật `closed_at >= entered_at` KHÔNG kiểm lại ở đây: dòng nhập đi qua `domain.ValidateTrade` như hai đường vào kia. Xác nhận điều đó đúng bằng cách đọc đường gọi của importer trong `backend/internal/service/import.go`; nếu nó KHÔNG gọi `ValidateTrade`, thêm kiểm tại chỗ này dùng `domain.ErrClosedBeforeEntered` và bọc bằng `rowErr("closed_at", ...)` để người dùng thấy số dòng.

- [ ] **Step 9: Ghi cột mới trong exporter**

Trong hàm `row` của `backend/internal/exporter/csv.go`, chèn ngay sau `e.Day,`:

```go
		closedAt(t.ClosedAt),
```

Thêm helper cạnh `moneyPtr`:

```go
// closedAt ghi RFC3339, hoặc ô RỖNG khi lệnh chưa đóng.
//
// RFC3339 chứ không phải định dạng ngày của cột Day: cột này mang phần giờ, và
// phần giờ là thứ duy nhất làm nó có ích. Mang sẵn offset nên nhập lại không
// phụ thuộc vào timezone của account lúc nhập.
func closedAt(t *time.Time) string {
	if t == nil {
		return ""
	}
	return t.UTC().Format(time.RFC3339)
}
```

- [ ] **Step 10: Chạy test, xác nhận PASS**

```bash
make test-pure
make test
make lint
```

Expected: PASS toàn bộ. Nếu một test cũ đỏ vì đếm số cột, sửa nó theo `csvformat.InputColumnCount` chứ không chép con số 19 vào test — đó đúng là điều mà comment của hằng số này cảnh báo.

- [ ] **Step 11: Commit**

```bash
git add backend/internal/csvformat/ backend/internal/importer/ backend/internal/exporter/
git commit -m "feat(be): cột CSV \"Ngày đóng\" giữ nguyên phần giờ

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: `formatDuration` và kiểu dữ liệu frontend

**Files:**
- Modify: `frontend/src/lib/format.ts`
- Create: `frontend/src/lib/formatDuration.test.ts`
- Modify: `frontend/src/features/trades/types.ts:12` (`Trade`), `:88-106` (`TradeCreate`), `:122-156` (`Stats`)
- Modify: `frontend/src/features/dashboard/types.ts` (thêm `HoldBucket` và trường `hold_distribution`)
- Modify: `frontend/src/test/tradeFactory.ts`
- Modify: `frontend/src/i18n/strings.ts`

**Interfaces:**
- Consumes: hợp đồng JSON của Task 5 và Task 4.
- Produces:
  - `formatDuration(seconds: number, locale: Locale): string`
  - `Trade.closed_at: string | null`, `Trade.hold_seconds: number | null`
  - `TradeCreate.closed_at: string | null` (và qua đó `TradePatch`, vốn là `Partial<TradeCreate>`)
  - `Stats.avg_hold_seconds`, `.avg_hold_seconds_win`, `.avg_hold_seconds_loss`: `number | null`
  - `HoldBucket = { label: string; count: number; wins: number; losses: number; sum_net: string }`
  - Khoá i18n: `kpi.avgHold`, `kpi.avgHoldWin`, `kpi.avgHoldLoss`, `table.holdTime`, `tradeForm.closedAt`, `tradeForm.closedAtBeforeEntered`, `tradeForm.closeTime`, `dashboard.holdDist`, `dashboard.holdBucket`.

- [ ] **Step 1: Viết test fail cho `formatDuration`**

`frontend/src/lib/formatDuration.test.ts`:

```ts
import { describe, expect, test } from "vitest";
import { formatDuration } from "./format";

describe("formatDuration", () => {
  test("dưới một phút hiện giây, không phần thập phân", () => {
    // Nửa giây không nói thêm được gì về một lệnh giữ 45 giây.
    expect(formatDuration(45, "vi")).toBe("45s");
    expect(formatDuration(0, "vi")).toBe("0s");
    expect(formatDuration(59, "vi")).toBe("59s");
  });

  test("phút hiện một chữ số thập phân, dấu phẩy theo locale vi", () => {
    expect(formatDuration(786, "vi")).toBe("13,1m");
    expect(formatDuration(60, "vi")).toBe("1,0m");
  });

  test("locale en dùng dấu chấm", () => {
    expect(formatDuration(786, "en")).toBe("13.1m");
  });

  test("từ một giờ trở lên hiện giờ", () => {
    expect(formatDuration(3600, "vi")).toBe("1,0h");
    expect(formatDuration(9000, "vi")).toBe("2,5h");
  });

  test("từ một ngày trở lên hiện ngày", () => {
    expect(formatDuration(86400, "vi")).toBe("1,0 ngày");
    expect(formatDuration(103680, "vi")).toBe("1,2 ngày");
  });

  test("biên khít với ngưỡng đơn vị kế tiếp", () => {
    // 3599s vẫn là phút, 3600s đã là giờ — không có khe nào giữa hai đơn vị.
    expect(formatDuration(3599, "vi")).toBe("60,0m");
    expect(formatDuration(86399, "vi")).toBe("24,0h");
  });
});
```

- [ ] **Step 2: Chạy test, xác nhận FAIL**

```bash
cd frontend && nvm use && npx vitest run src/lib/formatDuration.test.ts
```

Expected: FAIL — `formatDuration is not a function` / không export.

- [ ] **Step 3: Viết `formatDuration`**

Thêm vào `frontend/src/lib/format.ts`:

```ts
/**
 * Thời lượng (GIÂY) thành chuỗi ngắn đọc được: "45s", "13,1m", "2,5h",
 * "1,2 ngày".
 *
 * Một chữ số thập phân là chủ ý: đây là con số để LIẾC, không phải để đối
 * chiếu. "13,1m" trả lời đúng câu hỏi "tôi scalp hay swing"; "13m 6s" thì bắt
 * người đọc xử lý hai con số để trả lời cùng câu đó.
 *
 * Dưới một phút thì bỏ phần thập phân — nửa giây không thêm thông tin nào.
 *
 * Dấu thập phân theo locale qua Intl.NumberFormat, không nối chuỗi bằng tay:
 * locale vi dùng dấu phẩy, en dùng dấu chấm.
 */
export function formatDuration(seconds: number, locale: Locale = "vi"): string {
  if (seconds < 60) return `${Math.round(seconds)}s`;

  const nf = new Intl.NumberFormat(locale === "vi" ? "vi-VN" : "en-US", {
    minimumFractionDigits: 1,
    maximumFractionDigits: 1,
  });

  if (seconds < 3600) return `${nf.format(seconds / 60)}m`;
  if (seconds < 86400) return `${nf.format(seconds / 3600)}h`;
  return `${nf.format(seconds / 86400)} ${locale === "vi" ? "ngày" : "d"}`;
}
```

Nếu `Locale` chưa được import trong file này, đọc phần đầu file để dùng đúng import đang có (`formatDateOnly` cùng file đã nhận tham số `locale: Locale`).

- [ ] **Step 4: Chạy test, xác nhận PASS**

```bash
cd frontend && npx vitest run src/lib/formatDuration.test.ts
```

Expected: PASS, cả 6 nhóm.

- [ ] **Step 5: Khai kiểu dữ liệu**

`frontend/src/features/trades/types.ts`, trong `Trade` sau `entered_at`:

```ts
  /** ISO UTC, null khi lệnh chưa đóng. */
  closed_at: string | null;
  /** Thời gian giữ lệnh tính bằng GIÂY. null = chưa đóng, KHÁC 0. */
  hold_seconds: number | null;
```

Trong `TradeCreate`, sau `entered_at` — KHÔNG thêm `hold_seconds` vào đây, nó là
trường suy diễn, gửi lên sẽ bị backend từ chối bằng `DisallowUnknownFields`:

```ts
  /** null = lệnh chưa đóng. Không bắt buộc như `entered_at` ngay trên. */
  closed_at: string | null;
```

`TradePatch` là `Partial<TradeCreate>` nên nó tự có `closed_at?: string | null` —
đúng ba trạng thái mà backend chờ: khoá vắng = giữ nguyên, `null` = xoá giờ
đóng, chuỗi = đặt giờ đóng.

Trong `Stats`, sau `expectancy`:

```ts
  /**
   * Thời gian giữ lệnh trung bình, GIÂY. Số chứ không phải chuỗi: không phải
   * tiền nên không có rủi ro mất chữ số của quy tắc "tiền là chuỗi".
   * null = chưa có lệnh nào có giờ đóng, KHÁC 0 giây.
   */
  avg_hold_seconds: number | null;
  avg_hold_seconds_win: number | null;
  avg_hold_seconds_loss: number | null;
```

`frontend/src/features/dashboard/types.ts`, cạnh `RBucket`:

```ts
/** Một cột của histogram thời gian giữ. `sum_net` là TIỀN nên là chuỗi. */
export type HoldBucket = {
  label: string;
  count: number;
  wins: number;
  losses: number;
  sum_net: string;
};
```

Và thêm `hold_distribution: HoldBucket[];` vào kiểu `Charts` trong cùng file (tìm chỗ khai `r_distribution` để đặt cạnh).

- [ ] **Step 6: Cập nhật factory test và i18n**

`frontend/src/test/tradeFactory.ts`: thêm `closed_at: null,` và `hold_seconds: null,` vào object mặc định. Nếu có factory cho `Stats` hoặc `Charts` trong cùng thư mục, thêm ba trường KPI (`null`) và `hold_distribution: []`.

`frontend/src/i18n/strings.ts`, thêm các khoá (đặt cạnh nhóm khoá cùng tiền tố đang có):

```ts
  "kpi.avgHold": { vi: "Thời gian giữ TB", en: "Avg hold time" },
  "kpi.avgHoldWin": { vi: "Giữ TB lệnh thắng", en: "Avg hold (win)" },
  "kpi.avgHoldLoss": { vi: "Giữ TB lệnh thua", en: "Avg hold (loss)" },
  "table.holdTime": { vi: "Thời gian giữ", en: "Hold time" },
  "tradeForm.closedAt": { vi: "Thời điểm đóng lệnh", en: "Close time" },
  "tradeForm.closeTime": { vi: "Giờ đóng", en: "Close time" },
  "tradeForm.closedAtBeforeEntered": {
    vi: "thời điểm đóng lệnh không được trước thời điểm vào lệnh",
    en: "close time cannot be before entry time",
  },
  "dashboard.holdDist": { vi: "Phân phối theo thời gian giữ lệnh", en: "Hold time distribution" },
  "dashboard.holdBucket": { vi: "Khoảng thời gian giữ", en: "Hold bucket" },
```

- [ ] **Step 7: Kiểm kiểu và chạy test FE**

```bash
cd frontend && npx tsc --noEmit && npm run test
```

Expected: tsc sạch; test PASS. Nếu tsc báo thiếu `closed_at`/`hold_seconds` ở một mock nào đó, sửa mock — đó đúng là việc kiểu dữ liệu sinh ra để bắt.

- [ ] **Step 8: Commit**

```bash
git add frontend/src/lib/format.ts frontend/src/lib/formatDuration.test.ts \
        frontend/src/features/trades/types.ts frontend/src/features/dashboard/types.ts \
        frontend/src/test/tradeFactory.ts frontend/src/i18n/strings.ts
git commit -m "feat(fe): formatDuration và kiểu dữ liệu thời gian giữ lệnh

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 8: Ô nhập `closed_at` trong form lệnh

**Files:**
- Modify: `frontend/src/features/trades/TradeFormDialog.tsx` — `makeSchema` (:76-96), `transforms` (:324-346), `defaults` (:904-923), `fromTrade` (:925-945), và JSX băng "Đóng lệnh" (quanh :520)
- Test: `frontend/src/features/trades/tradeForm.test.tsx`

**Interfaces:**
- Consumes: `Trade.closed_at` (T7), hợp đồng `POST`/`PATCH` (T5).
- Produces: form gửi `closed_at` là chuỗi ISO UTC khi có, `null` khi ô rỗng.

- [ ] **Step 1: Viết test fail**

Thêm vào `frontend/src/features/trades/tradeForm.test.tsx`, theo khuôn của test `"thêm lệnh gửi entered_at đổi theo timezone của ACCOUNT"` đã có ở dòng 112 (đọc nó trước để dùng lại helper render/submit).

```tsx
test("ô đóng lệnh để trống thì gửi closed_at null", async () => {
  const submitted = await submitForm({
    /* điền các ô bắt buộc như test entered_at đang làm, KHÔNG chạm ô closed_at */
  });

  // null chứ không phải chuỗi rỗng: backend phân biệt "chưa đóng" với một
  // chuỗi rỗng không đọc được.
  expect(submitted.closed_at).toBeNull();
});

test("thêm lệnh gửi closed_at đổi theo timezone của ACCOUNT", async () => {
  const tz = "America/New_York";
  const submitted = await submitForm({ timezone: tz, closedAtWall: "2026-09-10T10:15" });

  expect(submitted.closed_at).toBe(wallToInstant("2026-09-10T10:15", tz));
});

test("đóng lệnh trước khi vào lệnh thì báo lỗi và KHÔNG gửi", async () => {
  const { submitted, errorText } = await trySubmitForm({
    enteredAtWall: "2026-09-10T14:00",
    closedAtWall: "2026-09-10T13:00",
  });

  expect(submitted).toBeUndefined();
  expect(errorText).toMatch(/không được trước/);
});
```

Điều chỉnh tên helper cho khớp với file thật; giữ nguyên ba khẳng định.

- [ ] **Step 2: Chạy test, xác nhận FAIL**

```bash
cd frontend && npx vitest run src/features/trades/tradeForm.test.tsx
```

Expected: FAIL — không tìm thấy ô `closed_at`.

- [ ] **Step 3: Thêm vào schema, có luật so sánh hai mốc**

Trong `makeSchema`, thêm field và một `superRefine` trên cả object (luật liên-trường không đặt được trên một field đơn lẻ):

```ts
function makeSchema(t: Translate) {
  return z.object({
    entered_at: z.string().min(1, t("tradeForm.enteredAtRequired")),
    // KHÔNG bắt buộc: lệnh còn đang chạy thì chưa có giờ đóng, và form này
    // phải ghi được một lệnh vừa mở — cùng lý do đã khiến `profit` bỏ ràng
    // buộc bắt buộc.
    closed_at: z.string(),
    // …giữ nguyên phần còn lại…
  }).superRefine((v, ctx) => {
    // So sánh trên chuỗi giờ TƯỜNG (cùng một timezone account cho cả hai ô)
    // nên so sánh từ điển là đúng thứ tự thời gian: cả hai đều dạng
    // "YYYY-MM-DDTHH:mm". Đổi sang instant rồi so cũng ra cùng kết quả nhưng
    // phải truyền timezone vào tận đây.
    if (v.closed_at !== "" && v.entered_at !== "" && v.closed_at < v.entered_at) {
      ctx.addIssue({
        code: z.ZodIssueCode.custom,
        path: ["closed_at"],
        message: t("tradeForm.closedAtBeforeEntered"),
      });
    }
  });
}
```

Lưu ý: `superRefine` trả `ZodEffects`, nên nếu `Fields` hay `zodResolver` vướng kiểu, giữ `z.object({...})` ở một biến rồi export cả hai (`const shape = z.object({...}); return shape.superRefine(...)`) và suy `Fields` từ `shape`.

- [ ] **Step 4: Thêm transform, defaults, fromTrade**

Trong `transforms`, cạnh `entered_at`:

```ts
    // Ô rỗng gửi null, khác entered_at ngay trên — ở đó ô rỗng là lỗi
    // validation nên không bao giờ tới được đây.
    closed_at: (x: string) => ({
      key: "closed_at" as const,
      value: x.trim() === "" ? null : wallToInstant(x, account.timezone),
    }),
```

Trong `defaults`, cạnh `entered_at`:

```ts
    // Rỗng chứ không phải nowInZone: mặc định của form là "vừa mở một lệnh",
    // và một giờ đóng điền sẵn sẽ ghi vào DB một con số người dùng chưa gõ.
    closed_at: "",
```

Trong `fromTrade`, cạnh `entered_at`:

```ts
    closed_at: t.closed_at === null ? "" : instantToWall(t.closed_at, tz),
```

- [ ] **Step 5: Thêm ô vào JSX băng "Đóng lệnh"**

Tìm `<Band label={translate("tradeForm.closeBand")}` (khoảng dòng 520) và thêm ô vào đầu lưới của băng đó, theo đúng khuôn ô `entered_at` ở băng "Mở lệnh":

```tsx
            <div className="flex flex-col gap-1.5">
              <FieldLabel htmlFor="closed-at" label={translate("tradeForm.closedAt")} />
              <Controller
                control={control}
                name="closed_at"
                render={({ field }) => (
                  <DateTimePicker
                    id="closed-at"
                    value={field.value}
                    onChange={field.onChange}
                    onBlur={field.onBlur}
                    placeholder={translate("tradeForm.chooseDateTime")}
                    ariaLabel={translate("tradeForm.closedAt")}
                    timeLabel={translate("tradeForm.closeTime")}
                    aria-invalid={Boolean(errors.closed_at)}
                  />
                )}
              />
              <FieldFoot errorId="closed-at-error" errorMsg={errors.closed_at?.message} />
            </div>
```

`FieldLabel` không có `required` — ô này không bắt buộc, và băng "Đóng lệnh" đã mang sẵn ghi chú "chưa cần điền nếu lệnh còn đang chạy" (xem comment ở dòng 425).

- [ ] **Step 6: Chạy test, xác nhận PASS**

```bash
cd frontend && npx vitest run src/features/trades/tradeForm.test.tsx && npx tsc --noEmit
```

Expected: PASS cả ba test mới, tsc sạch.

- [ ] **Step 7: Commit**

```bash
git add frontend/src/features/trades/TradeFormDialog.tsx frontend/src/features/trades/tradeForm.test.tsx
git commit -m "feat(fe): ô nhập thời điểm đóng lệnh trong form

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 9: Cột "Thời gian giữ" trong bảng lệnh

**Files:**
- Modify: `frontend/src/features/trades/TradeTable.tsx:119` (header), `:138` (ô), `:245-301` (khối chi tiết mở rộng)
- Test: `frontend/src/features/trades/tradeTable.test.tsx`

**Interfaces:**
- Consumes: `Trade.hold_seconds` (T7), `formatDuration` (T7), khoá `table.holdTime` (T7).
- Produces: không có gì cho task sau.

- [ ] **Step 1: Viết test fail**

Thêm vào `frontend/src/features/trades/tradeTable.test.tsx`:

```tsx
test("cột thời gian giữ hiện thời lượng đã format", () => {
  renderPage([makeTrade({ hold_seconds: 786 })], "Asia/Ho_Chi_Minh");
  expect(screen.getByText("13,1m")).toBeInTheDocument();
});

test("lệnh chưa đóng hiện — chứ không phải 0s", () => {
  renderPage([makeTrade({ hold_seconds: null })], "Asia/Ho_Chi_Minh");
  // "0s" ở đây sẽ đọc thành "vào ra tức thì", một câu trả lời sai trông như
  // một câu trả lời thật.
  expect(screen.queryByText("0s")).not.toBeInTheDocument();
});
```

- [ ] **Step 2: Chạy test, xác nhận FAIL**

```bash
cd frontend && npx vitest run src/features/trades/tradeTable.test.tsx
```

Expected: FAIL — không tìm thấy `"13,1m"`.

- [ ] **Step 3: Thêm cột**

Trong `TradeTable.tsx`, sau `<TableHead>{translate("table.enteredAt")}</TableHead>`:

```tsx
            <TableHead>{translate("table.holdTime")}</TableHead>
```

Sau ô `{formatInstant(t.entered_at, timezone, locale)}` (dòng 138), thêm một `<TableCell>` tương ứng — đọc file để khớp đúng cách các ô khác được bọc và class `num` mà chúng dùng:

```tsx
                  <TableCell className="num">
                    {t.hold_seconds === null ? noValue : formatDuration(t.hold_seconds, locale)}
                  </TableCell>
```

Thêm `formatDuration` vào import từ `@/lib/format`. `noValue` đã có sẵn trong file (dùng ở các ô `NumberCell`).

Trong khối chi tiết mở rộng, thêm một dòng `<Close>` cạnh dòng ngày/tuần/tháng:

```tsx
          <Close
            label={translate("table.holdTime")}
            value={t.hold_seconds === null ? noValue : formatDuration(t.hold_seconds, locale)}
          />
```

- [ ] **Step 4: Chạy test, xác nhận PASS**

```bash
cd frontend && npx vitest run src/features/trades/tradeTable.test.tsx && npx tsc --noEmit
```

Expected: PASS. Nếu một test cũ đếm số cột của bảng đỏ, sửa con số đó — cột mới là thay đổi có chủ ý.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/features/trades/TradeTable.tsx frontend/src/features/trades/tradeTable.test.tsx
git commit -m "feat(fe): cột thời gian giữ lệnh trong bảng

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 10: Ba ô KPI thời gian giữ trên dashboard

**Files:**
- Modify: `frontend/src/features/dashboard/KpiGrid.tsx`
- Test: `frontend/src/features/dashboard/kpiGrid.test.tsx`

**Interfaces:**
- Consumes: `Stats.avg_hold_seconds*` (T7), `formatDuration` (T7), khoá `kpi.avgHold*` (T7).
- Produces: không có gì cho task sau.

- [ ] **Step 1: Viết test fail**

Thêm vào `frontend/src/features/dashboard/kpiGrid.test.tsx`, theo khuôn bảng `["Kỳ vọng mỗi lệnh", "expectancy"]` ở dòng 43:

```tsx
test("ba ô thời gian giữ hiện thời lượng đã format", () => {
  renderGrid({ avg_hold_seconds: 786, avg_hold_seconds_win: 512, avg_hold_seconds_loss: 1893 });

  expect(screen.getByText("13,1m")).toBeInTheDocument();
  expect(screen.getByText("8,5m")).toBeInTheDocument();
  expect(screen.getByText("31,6m")).toBeInTheDocument();
});

test("chưa có lệnh nào đóng thì ba ô hiện —", () => {
  renderGrid({ avg_hold_seconds: null, avg_hold_seconds_win: null, avg_hold_seconds_loss: null });

  // Ba dấu — chứ không phải ba số 0.
  expect(screen.getAllByText("—").length).toBeGreaterThanOrEqual(3);
});
```

Điều chỉnh helper `renderGrid` cho khớp file thật.

- [ ] **Step 2: Chạy test, xác nhận FAIL**

```bash
cd frontend && npx vitest run src/features/dashboard/kpiGrid.test.tsx
```

Expected: FAIL — không tìm thấy `"13,1m"`.

- [ ] **Step 3: Thêm ba ô**

`Cell` sẵn có nhận `v: string | null`, còn ba trường này là `number | null` — nên viết một cell riêng thay vì ép kiểu:

```tsx
/**
 * Ô thời lượng. Tách khỏi `Cell` vì giá trị là SỐ, không phải chuỗi thập phân:
 * ép number qua Cell sẽ mất cái cổng `v === null` mà Cell dựng lên để phân
 * biệt "không tính được" với 0.
 */
function DurationCell({ seconds, locale }: { seconds: number | null; locale: Locale }) {
  if (seconds === null) return <span className="num text-lg text-muted-foreground">—</span>;
  return <span className="num text-lg font-medium">{formatDuration(seconds, locale)}</span>;
}
```

Đặt ba ô cạnh nhau trong `StatGrid` (sau ô `kpi.expectancy` — chúng cùng là chỉ số về HÀNH VI giao dịch, khác nhóm số dư/tiền ở đầu lưới):

```tsx
        <StatTile label={t("kpi.avgHold")}>
          <DurationCell seconds={s.avg_hold_seconds} locale={locale} />
        </StatTile>
        <StatTile label={t("kpi.avgHoldWin")}>
          <DurationCell seconds={s.avg_hold_seconds_win} locale={locale} />
        </StatTile>
        <StatTile label={t("kpi.avgHoldLoss")}>
          <DurationCell seconds={s.avg_hold_seconds_loss} locale={locale} />
        </StatTile>
```

KHÔNG tô màu lãi/lỗ cho ba ô này: thời gian giữ lâu không phải "tốt" hay "xấu" tự thân, và tô teal/đỏ ở đây sẽ khẳng định một điều mà dữ liệu không nói.

Thêm import `formatDuration` và kiểu `Locale`. Cập nhật comment đầu file: "Đủ 24 chỉ số" → con số mới.

- [ ] **Step 4: Chạy test, xác nhận PASS**

```bash
cd frontend && npx vitest run src/features/dashboard/kpiGrid.test.tsx && npx tsc --noEmit
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/features/dashboard/KpiGrid.tsx frontend/src/features/dashboard/kpiGrid.test.tsx
git commit -m "feat(fe): ba ô KPI thời gian giữ lệnh

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 11: Biểu đồ phân phối thời gian giữ lệnh

**Files:**
- Create: `frontend/src/features/dashboard/HoldTimeChart.tsx`
- Create: `frontend/src/features/dashboard/holdTimeChart.test.tsx`
- Modify: `frontend/src/features/dashboard/prepare.ts`
- Modify: `frontend/src/features/dashboard/prepare.test.ts`
- Modify: `frontend/src/features/dashboard/DashboardPage.tsx`

**Interfaces:**
- Consumes: `HoldBucket` (T7), `Charts.hold_distribution` (T7), `ChartCard`, `chartTheme` (sẵn có).
- Produces: `<HoldTimeChart rows={charts.hold_distribution} />`.

- [ ] **Step 1: Viết test fail cho `prepareHoldDist`**

Thêm vào `frontend/src/features/dashboard/prepare.test.ts`:

```ts
test("prepareHoldDist giữ nguyên thứ tự bucket và đổi sum_net thành số", () => {
  const rows = [
    { label: "< 5m", count: 3, wins: 2, losses: 1, sum_net: "120.5" },
    { label: "5m – 15m", count: 0, wins: 0, losses: 0, sum_net: "0" },
  ];

  const got = prepareHoldDist(rows);

  // Thứ tự là TRỤC của biểu đồ, không được sắp lại theo count.
  expect(got.map((d) => d.label)).toEqual(["< 5m", "5m – 15m"]);
  // sum_net vào Recharts nên phải là number; chuỗi sẽ vẽ ra một trục hạng mục.
  expect(got[0].sumNet).toBe(120.5);
});
```

- [ ] **Step 2: Chạy test, xác nhận FAIL**

```bash
cd frontend && npx vitest run src/features/dashboard/prepare.test.ts
```

Expected: FAIL — `prepareHoldDist is not exported`.

- [ ] **Step 3: Viết `prepareHoldDist`**

Thêm vào `frontend/src/features/dashboard/prepare.ts`, cạnh `prepareRDist`:

```ts
export type HoldCol = {
  label: string;
  count: number;
  wins: number;
  losses: number;
  /**
   * Recharts chỉ vẽ được number, nên tiền phải qua Number() ở ĐÚNG một chỗ —
   * ngay tại ranh giới vào biểu đồ. Mọi chỗ khác giữ chuỗi theo quy tắc 1.
   * Sai số dấu phẩy động ở đây vô hại: nó chỉ quyết định chiều cao một cột,
   * còn con số người dùng đọc lấy từ chuỗi gốc qua tooltip và bảng.
   */
  sumNet: number;
};

export function prepareHoldDist(rows: HoldBucket[]): HoldCol[] {
  return rows.map((r) => ({
    label: r.label,
    count: r.count,
    wins: r.wins,
    losses: r.losses,
    sumNet: Number(r.sum_net),
  }));
}
```

Thêm import `HoldBucket` từ `./types`.

- [ ] **Step 4: Chạy test, xác nhận PASS**

```bash
cd frontend && npx vitest run src/features/dashboard/prepare.test.ts
```

Expected: PASS.

- [ ] **Step 5: Viết test fail cho component**

`frontend/src/features/dashboard/holdTimeChart.test.tsx` — theo khuôn `rDistributionChart`/`tradeClassChart` test đang có (đọc một trong hai trước để dùng đúng wrapper i18n và cách Recharts được mock/đo trong repo này):

```tsx
test("không có lệnh nào có giờ đóng thì hiện trạng thái rỗng", () => {
  renderChart([
    { label: "< 5m", count: 0, wins: 0, losses: 0, sum_net: "0" },
    { label: "5m – 15m", count: 0, wins: 0, losses: 0, sum_net: "0" },
  ]);

  // Sáu cột cao 0 trông như một biểu đồ hỏng; trạng thái rỗng nói đúng sự thật.
  expect(screen.getByText(/chưa có dữ liệu/i)).toBeInTheDocument();
});

test("bảng phụ liệt kê đủ bucket kèm thắng thua", () => {
  renderChart([{ label: "< 5m", count: 3, wins: 2, losses: 1, sum_net: "120.5" }]);

  expect(screen.getByText("< 5m")).toBeInTheDocument();
});
```

- [ ] **Step 6: Chạy test, xác nhận FAIL**

```bash
cd frontend && npx vitest run src/features/dashboard/holdTimeChart.test.tsx
```

Expected: FAIL — module không tồn tại.

- [ ] **Step 7: Viết `HoldTimeChart.tsx`**

```tsx
import { Bar, BarChart, CartesianGrid, Tooltip, XAxis, YAxis } from "recharts";
import { useI18n } from "@/i18n";
import { ChartCard } from "./ChartCard";
import { BAR_CURSOR, TOOLTIP_STYLE } from "./chartTheme";
import { prepareHoldDist } from "./prepare";
import type { HoldBucket } from "./types";

/**
 * Phân phối lệnh theo khoảng thời gian giữ — cột CHỒNG hai màu thắng/thua.
 *
 * Khác RDistributionChart ở chỗ này, và khác có lý do: ở đó R = net / one_R
 * nên dấu của R luôn bằng dấu của net, mỗi bucket chỉ có một cực tính và cột
 * chồng sẽ mãi mãi chỉ có một tầng. Ở đây một khoảng thời gian chứa CẢ lệnh
 * thắng lẫn lệnh thua — và tỉ lệ hai tầng trong cùng một cột chính là câu trả
 * lời cho "giữ lệnh bao lâu thì tôi thắng".
 *
 * Lãi = --primary (teal), lỗ = --status-error (đỏ), theo Theme của CLAUDE.md.
 */
export function HoldTimeChart({ rows }: { rows: HoldBucket[] }) {
  const { t } = useI18n();
  const data = prepareHoldDist(rows);
  const hasData = data.some((d) => d.count > 0);

  return (
    <ChartCard
      title={t("dashboard.holdDist")}
      empty={!hasData}
      height="h-64"
      table={{
        col: [
          t("dashboard.holdBucket"),
          t("dashboard.tradeCount"),
          t("dashboard.wins"),
          t("dashboard.losses"),
        ],
        row: data.map((d) => [d.label, d.count, d.wins, d.losses]),
      }}
    >
      <BarChart data={data} margin={{ top: 4, right: 4, bottom: 24, left: 4 }}>
        <CartesianGrid strokeDasharray="3 3" vertical={false} stroke="var(--border-default)" />
        <XAxis dataKey="label" tick={{ fontSize: 11 }} stroke="var(--text-muted)" interval={0} />
        <YAxis tick={{ fontSize: 12 }} stroke="var(--text-muted)" width={40} allowDecimals={false} />
        <Tooltip cursor={BAR_CURSOR} contentStyle={TOOLTIP_STYLE} />
        <Bar dataKey="wins" stackId="hold" fill="var(--primary)" name={t("dashboard.wins")} />
        <Bar dataKey="losses" stackId="hold" fill="var(--status-error)" name={t("dashboard.losses")} />
      </BarChart>
    </ChartCard>
  );
}
```

Đọc `ChartCard.tsx` và `chartTheme.tsx` trước để khớp đúng props thật (tên prop `table`, `empty`, `height` lấy từ `RDistributionChart`; nếu khác thì theo file thật). Kiểm tra `dashboard.tradeCount`/`dashboard.wins`/`dashboard.losses` đã có trong `strings.ts` — `RDistributionChart` đang dùng chúng nên rất có thể đã có.

- [ ] **Step 8: Nối vào `DashboardPage`**

Đặt `<HoldTimeChart rows={charts.hold_distribution} />` cạnh `<RDistributionChart>` trong `DashboardPage.tsx`, theo đúng cách các chart khác được bọc trong section/grid ở đó.

- [ ] **Step 9: Chạy toàn bộ kiểm tra FE, xác nhận PASS**

```bash
cd frontend && npx tsc --noEmit && npm run test && npm run build
```

Expected: tsc sạch, test PASS, build thành công.

- [ ] **Step 10: Commit**

```bash
git add frontend/src/features/dashboard/HoldTimeChart.tsx \
        frontend/src/features/dashboard/holdTimeChart.test.tsx \
        frontend/src/features/dashboard/prepare.ts \
        frontend/src/features/dashboard/prepare.test.ts \
        frontend/src/features/dashboard/DashboardPage.tsx
git commit -m "feat(fe): biểu đồ phân phối theo thời gian giữ lệnh

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 12: Kiểm tra toàn hệ và cập nhật tài liệu

**Files:**
- Modify: `CLAUDE.md` (nếu cần) — mục "Quy tắc bất di bất dịch" có nhắc danh sách trường suy diễn ở quy tắc 2
- Modify: `docs/superpowers/specs/2026-08-16-trading-journal-design.md` (mục mô tả bảng `trades` và danh sách trường suy diễn)

- [ ] **Step 1: Chạy toàn bộ cổng kiểm tra**

```bash
make lint
make test
cd frontend && nvm use && npx tsc --noEmit && npm run test && npm run build
```

Expected: tất cả PASS. Ghi lại kết quả THẬT — nếu có test đỏ thì sửa trước khi đi tiếp, không báo "xong".

- [ ] **Step 2: Kiểm tra thủ công trên stack thật**

```bash
make up-dev
```

Mở app, rồi kiểm ĐỦ năm điểm:

1. Thêm một lệnh có giờ đóng → bảng hiện đúng thời gian giữ.
2. Thêm một lệnh KHÔNG có giờ đóng → cột hiện `—`, không phải `0s`.
3. Dashboard: ba ô KPI có số, biểu đồ phân phối có cột.
4. Xuất CSV → mở file, cột "Ngày đóng" có dấu thời gian đầy đủ.
5. Nhập lại chính file đó → thời gian giữ lệnh KHÔNG đổi.

- [ ] **Step 3: Cập nhật quy tắc 2 của CLAUDE.md**

Quy tắc 2 liệt kê tên các trường suy diễn. Thêm `hold_seconds` vào danh sách đó:

```markdown
2. **Không lưu trường suy diễn.** `net`, `score_*`, `cum_*`, `drawdown`, `week`, `month`,
   `weekday`, `day`, `hold_seconds` đều tính lúc đọc, không có cột trong DB.
```

- [ ] **Step 4: Cập nhật spec hệ thống**

Trong `docs/superpowers/specs/2026-08-16-trading-journal-design.md`, tìm mục mô tả bảng `trades` và mục liệt kê trường suy diễn; thêm `closed_at` vào phần cột DB và `hold_seconds` vào phần suy diễn, kèm một dòng trỏ sang `docs/superpowers/specs/2026-09-11-hold-time-design.md`.

- [ ] **Step 5: Commit**

```bash
git add CLAUDE.md docs/superpowers/specs/2026-08-16-trading-journal-design.md
git commit -m "docs: ghi nhận closed_at và hold_seconds vào spec hệ thống

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Ghi chú cho người thực thi

**Thứ tự task không đổi được ở ba chỗ:** T1 → T2 → T3 (mỗi cái dựng trên kiểu dữ liệu của cái trước), T5 phải sau T3 và T4 (nó phơi cả hai ra JSON), T7 phải trước T8–T11 (chúng dùng kiểu và khoá i18n do nó khai). T6 độc lập với T7–T11, chạy song song được.

**Test của `httpapi`, `repository`, `service` CẦN Docker** (testcontainers). Không có Docker thì `make test-pure` chạy được T1–T4 và T6; T5 phải chờ.

**Bẫy đã biết:**

- `DecodeJSON` bật `DisallowUnknownFields`, nên `closed_at` chưa khai trong request struct sẽ làm request 400 chứ không bị bỏ qua — đó là lý do test ở T5 Step 1 fail đúng cách.
- `importer.ParseDay` cắt bỏ phần giờ có chủ ý. Dùng lại nó cho `closed_at` là cái bẫy lớn nhất của plan này, và `TestRoundTripGiuNguyenClosedAtKeCaPhanGio` ở T6 là thứ canh nó.
- `csvformat.InputColumnCount` có test cấu trúc canh ranh giới input/derived (`TestInputColumnCountPointsAtCorrectBoundary`). Quên sửa hằng số này thì hai test khác sẽ khẳng định ngược điều chúng định khẳng định — im lặng.
- Node mặc định của shell là v16 và làm `tsc`/`vitest` crash. `nvm use` trước mọi lệnh frontend.
